apiVersion: v1
kind: Service
metadata:
  name: traveloop
  labels:
    app: traveloop
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: traveloop
